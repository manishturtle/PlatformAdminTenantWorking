import type { NextPage } from 'next';
import LoginScreen from '@/components/auth/LoginScreen';

const CustomerLoginPage = () => {
    return <LoginScreen />;
};

export default CustomerLoginPage as NextPage;
